# 技術的視点からのソフトウェア分析

このソフトウェアは、PDFファイル内のテキストを抽出し、指定された言語に翻訳した後、元のテキストを削除したPDF上に翻訳済みテキストをオーバーレイ（重ねて表示）する一連の処理を自動化するコマンドラインツールである。

主要な処理は以下の4つのモジュールによって実現されている。

### 1. `main.py`: オーケストレーター

- **役割**: 全体の処理フローを制御するエントリーポイント。
- **実装**:
    - `argparse` を利用して、入力PDFパス、出力先、翻訳言語（英→日、日→英）、キャッシュ使用の有無をコマンドライン引数として受け取る。
    - `cache.py` の `build_or_load_cache` を呼び出し、テキストの抽出と翻訳を実行する。キャッシュが存在すればAPIコールをスキップし、なければ `translation.py` の機能を使って翻訳を実行し、結果をキャッシュする。
    - `pdf_text_remover.py` の `remove_page_text_keep_images_graphics` と `scrub_non_page_text` を順に呼び出し、元のPDFからテキスト情報のみを物理的に削除した中間PDFを生成する。
    - `translation.py` の `overlay_translations` を呼び出し、テキスト削除済みPDF上に、翻訳済みテキストを `FreeText` アノテーションとして元の位置・スタイル（フォント、サイズ、色など）を模倣して配置する。
    - 一時ファイルをクリーンアップする。

### 2. `translation.py`: テキスト抽出・翻訳・オーバーレイ

- **役割**: PDFからのテキスト情報抽出、外部APIによる翻訳、翻訳済みテキストのPDFへの再描画を担当する。
- **実装**:
    - **テキスト抽出 (`extract_spans`)**: `PyMuPDF (fitz)` を使用。PDFをページ、ブロック、ライン、スパンという階層で解析し、テキスト片（スパン）ごとに詳細な情報（テキスト内容、BBox座標、フォント、サイズ、色など）を抽出する。各スパンには一意のID (`pX_bX_lX_sX`) が付与される。
    - **翻訳 (`translate_spans_openai_async`)**:
        - `langchain-openai` を利用し、OpenAIのAPI（モデルは `gpt-4.1-mini` がデフォルト）を呼び出して翻訳を実行する。
        - `asyncio` と `tqdm.asyncio` を活用し、多数のテキストスパンをバッチに分割して非同期で並列処理することで、全体の処理時間を短縮している。
        - プロンプトエンジニアリングにより、翻訳結果が厳密なJSON形式 (`{"translations": [{"id": ..., "translated": ...}]}`) で返されるようモデルに指示している。`JsonOutputParser` で結果をパースする。
    - **オーバーレイ (`overlay_translations`)**: `PyMuPDF (fitz)` を使用。テキストが削除されたPDFに対し、キャッシュから取得した翻訳済みテキストと元のスタイル情報（座標、フォントサイズ、色など）を使って `FreeText` アノテーションを各スパンの位置に追加する。テキストの長さに応じてアノテーションの幅を動的に調整するロジックも含まれる。

### 3. `pdf_text_remover.py`: テキスト物理削除

- **役割**: PDFからテキスト描画命令を物理的に削除し、画像やベクターグラフィックスは保持する。
- **実装**:
    - **ページ上のテキスト削除 (`remove_page_text_keep_images_graphics`)**: `PyMuPDF (fitz)` のリダクション（墨消し）機能を利用。ページ全体を墨消し対象にしつつ、`images` と `graphics` の削除を無効化することで、テキストのみを効率的に削除する。
    - **ページ外のテキスト削除 (`scrub_non_page_text`)**: `pikepdf` を使用し、より低レベルなPDF構造にアクセスする。
        - フォームフィールド (`AcroForm`)、アノテーション (`Annots`)、XObject（再利用可能なコンテンツ）、メタデータ（しおり等）など、ページコンテンツストリーム以外の場所に存在するテキスト情報も徹底的に探索し、除去する。
        - コンテンツストリームをパースし、テキスト描画オペレータ (`Tj`, `TJ` など) をストリームから直接取り除くという非常に高度な操作を行っている。

### 4. `cache.py`: キャッシュ管理

- **役割**: 翻訳結果をファイルベースでキャッシュし、再実行時のAPIコールを削減して処理を高速化・コストを低減する。
- **実装**:
    - 入力PDFファイルの内容自体を `SHA256` でハッシュ化し、キャッシュのキーとして利用する。これにより、ファイル名が異なっても内容が同じであればキャッシュがヒットする。
    - キャッシュが存在しない場合、`translation.py` を呼び出してテキストの抽出と翻訳を行い、スパンIDをキーとした詳細な情報（元のスタイル属性＋翻訳文）をJSON形式で `translation_cache` ディレクトリに保存する。
    - キャッシュが存在する場合は、ファイルからJSONを読み込んでそのまま返す。

### 総括

このソフトウェアは、複数のPDF操作ライブラリ (`PyMuPDF`, `pikepdf`) とAI翻訳API (`OpenAI` + `LangChain`) を巧みに組み合わせ、複雑なPDF翻訳タスクを自動化している。特に、テキストの物理的削除と、非同期処理による翻訳の高速化、ファイル内容ベースの堅牢なキャッシュ戦略が技術的な特徴と言える。
